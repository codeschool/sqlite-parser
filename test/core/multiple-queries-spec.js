import * as tree from '../helpers';

describe('multiple queries', function() {

  it('multiple queries 1', function(done) {
    tree.equals(this, done);
  });

  it('multiple queries 2', function(done) {
    tree.equals(this, done);
  });

});
